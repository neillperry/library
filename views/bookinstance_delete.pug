extends layout

block content
  h1 #{title}: #{bookinstance._id}
  p= bookinstance.book.title
  p= bookinstance.status

    p Do you really want to delete this Book Instance?

    form(method='POST' action='')
      div.form-group
        input#bookinstance._id.form-control(type='hidden',name='bookinstance._id', required='true', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete
